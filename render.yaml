services:
  - type: web
    name: my-web-service
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python -m build
    startCommand: |
      python -m installer dist/*.whl
